services:
  - type: web
    name: flare-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: ZOHO_USER
        sync: false
      - key: ZOHO_PASS
        sync: false
      - key: RELAY_SECRET
        sync: false
